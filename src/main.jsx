import React from "react";
import ReactDOM from "react-dom/client";
import TrainingsApp from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrainingsApp />
  </React.StrictMode>
);